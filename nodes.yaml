nodes:
  # List of remote nodes for distributed task execution
  remote_nodes:
    # Example node configuration
    # - name: worker1
    #   host: 192.168.1.100
    #   port: 22
    #   username: worker
    #   key_file: ~/.ssh/worker_key  # Path to SSH private key
    #   cores: 4  # Number of cores to use on this node 